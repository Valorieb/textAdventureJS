import { Card } from "@mui/material";

export const Story = () => {
  return (
    <Card>
      <div>
        <p>The story will start here</p>
        {/* Your adventure content */}
        {/* Use the Choice component to display and handle choices */}
      </div>
    </Card>
  );
};
